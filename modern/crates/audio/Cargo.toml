[package]
name = "audio"
description = "Audio engine: WAV playback and MIDI synthesis for 3D Movie Maker"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
engine = { workspace = true }
log = { workspace = true }
# rodio, cpal, fluidlite will be added in Phase 4
